# Browser key: enable "Maps JavaScript API" in Google Cloud Console.
# Restrict key by HTTP referrer (e.g. http://localhost:5173/*, http://localhost:3001/*).
# Rebuild the client after changing: npm run build
VITE_GOOGLE_MAPS_API_KEY=
