PORT=3001

# Optional: YouTube Data API v3 — enables video list in the UI (server-side call).
YOUTUBE_API_KEY=

# Google Maps: use client/.env with VITE_GOOGLE_MAPS_API_KEY (Maps JavaScript API).
# This server file does not need a Google Maps key for the current app.
